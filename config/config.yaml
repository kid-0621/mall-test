# ============================================
# mall 接口测试 — 环境配置
# ============================================

# 后端服务地址（你 IDEA 里跑的 mall-admin）
base_url: "http://localhost:8080"

# 登录账号信息
login:
  username: "admin"
  password: "macro123"

# 全局请求超时时间（秒）
timeout: 10
